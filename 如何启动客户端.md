# 启动 earcon 客户端

## 方式一：双击（推荐）

在 Finder 里进入 `earcon` 文件夹，双击 **`启动客户端.sh`**。

> 第一次双击可能被 macOS 拦截（未签名的脚本）：
> 右键 → 打开 → 打开。之后就可以正常双击了。

## 方式二：终端

```bash
cd ~/自进化模型/earcon
python3 desktop/app.py
```

## 前提

客户端依赖本地网关（127.0.0.1:8800）。如果窗口打开全是空的：

```bash
cd ~/自进化模型/earcon
python3 -m earcon.cli serve \
  --judge-upstream http://llm-gw.jd.local/v1 \
  --judge-api-key 5666589dcfbf476a949c764e963925e7 \
  --judge-model DeepSeek-V4-Pro-joybuilder \
  --routes-from zcode --port 8800 --db earcon_memory.db
```

客户端会自动检测网关，没在跑会尝试拉起。
